---
tags:
  - cheatsheet
  - math
  - 线代
  - 考研
---
> [!abstract]
> - 线性方程组 $Ax = b$
> 	- $b = 0$ 齐次线性方程组
> 		- 我们关心两个互补空间 $ker(A)$ 和 $row(A)$
> 	- $b \neq 0$ 非齐次线性方程组，蕴含着齐次线性方程组。
> 		- 退化到 $Ax= 0$  对于 $A_{m \times n}$ 也就是 $m$ 个方程 $n$ 个未知数
> 			- $r(A) + ker(A) = n$
> 			- $ker(A)$ 就是解空间的维数
> 			- 特别是 复杂的非齐次线性方程组，一定要考虑是否能够退化。
> 		- 我们有 $Ax_{1} = 0$进一步思考 $b = Ax_{2}$
> 			- 等价于 $Ax_{2} = Ax_{1} + b$ 应当理解为齐次方程组解空间平移。关键在于找到一个平移偏量。
> 		- 非齐次线性方程组没有解空间这一概念，我们转而应该从向量组上进行直观理解。对于$Ax = b$
> 			- $A$ 的列向量，经过 $x$ 对应的线性组合 能够表示 $b$
> 			- 对于非齐次线性方程组 $Ax = b$ 他等价于 列向量 $b$ 落在 $col(A)$ 当中。这提示我们从列向量组角度思考非齐次线性方程组。
> 	- 同解方程组 $A^{T}A = 0$ [[线性代数字典#^pqd5f8|同解和四秩相等]]
> 	- 
> - 广义上来看， $x^{T}A = b^{T},xa^{T} = b$ 其中 $a,x$ 都是列向量。也应当被理解为线性方程组。计算上来看，应该通过转置回到我们熟悉的操作。当然，向量的 内积，外积：
> 	- $a^{T}x$ , $ax^{T}$ 
> 	- $AA^{\star} = x$
> 		- 注意 $r(A^{\star})=1 \to r(A) = n-1$ 
> 		- 这里主要是 $3$ 阶或者 $4$ 阶的矩阵，给出条件可以适当写出来 判断。
> 		- 解空间都来自于 $A$ $A^{\star}$ 的列向量，要注意如何分辨线性无关。
> - 解的情况，回到空间上，用秩来辅助。理解上可以到向量组，和方程。
> 	- 进一步的，系数矩阵 $A$ 对应 $A^{T},AA^{T},A^{\star},A^{-1}$  的 线性方程组。
> 	- 同解、公共解（子集） 应该 利用 联立方程组得到的新的 方程组
> 	- 我们要充分利用：
> 		- $r(A) = r\begin{pmatrix}A \\ B\end{pmatrix} \iff$ $B$  的 行落在 $A$ 的行空间当中。也就是 $row(B) \subseteq row(A)$ 因此 ，我们利用完备性，$ker(A) \subseteq ker(B)$  也就是 齐次方程组 $Ax = 0$ 的解 都是 齐次方程组 $Bx = 0$ 的解。
> 		- 如果又有 $r(B) = r(A) \to r(B) = r\begin{pmatrix}A \\ b\end{pmatrix}$ 我们也可以同理得到 $row(A) \subseteq row(B) \to ker(B) \subseteq ker(A)$. 
> 		- 也就是 $row(A) = row(B), ker(A) = ker(B)$
> 		- 这里和我们之前说的向量组等价（列向量组）等价是如出一辙的，只不过这里多绕了一步，得到正交空间的关系。
> - 不要怕计算，考研数学三中有非常大的计算考核倾向。

# 求解参数矩阵

> [!tip] 参数矩阵
> 1. 将参数矩阵转化为未知数，回到求解方程的形式
> 2. 相似对角化逆运算，求出所有特征向量和所有的特征值，$P\Lambda P^{-1}$ 
> 3. 非齐次要分解为 齐次和非齐次
> 4. 线性方程组的两种形式，行与列

## 齐次矩阵方程


设 $A = \begin{pmatrix} 1 & 2 & 1 \\ 0 & 1 & a \\ 1 & a & 0\end{pmatrix}$ 三阶非零矩阵B满足 $BA = \mathbf{O}$ 


显然$A$ 的第一二列不成比例的，$r(A) \ge 2$  而 $B \neq \mathbf{O} \to r(B) \ge 1$   又有：$r(B)+r(A) = 3$ 因此：

$$
r(B) = 1, r(A) =2
$$


因此 $a = 1$ 

进一步，要求解 B，我们的B是根据矩阵方程得到的：

$$
BA = \mathbf{O}
$$

这里未知的 $B$ 在 左边，而非右边，转换到我们求解 $Ax = 0$  上：

$$
BA = \mathbf{O} \to (Ba_{1} ,Ba_{2},Ba_{3}) = \mathbf{O} \tag{1}
$$
我们设 $B = (b_{1},b_{2},b_{3})$  ，根据（1）我们可以得到三个方程：

$$
\begin{cases}
b_{1} + b_{3} = 0 \\
2b_{1} + b_{2} + b_{3} = 0 \\
b_{1} + b_{2}  = 0
\end{cases}
$$

再转换到：

$$
\begin{pmatrix}
1 & 0 & 1 \\
2 & 1 & 1 \\
1 & 1 & 0
\end{pmatrix} \begin{pmatrix}
b_{1} \\
b_{2} \\
b_{3}
\end{pmatrix} = 0
$$

观察力惊人，我们应该得到：

$$
A^{T}B^{T} = 0
$$

转置操作实际是我们的矩阵方程或者说矩阵等式的常见操作。 ^dn7faq

对于 $A^{T}$ 我们化简到：

$$
\begin{pmatrix}
1 & 0 & 1 \\
0 & 1 & -1 \\
0 & 0 & 0
\end{pmatrix}
$$

基础解系为：

$$
t\begin{pmatrix}
-1 \\
1 \\
1
\end{pmatrix}
$$

那么 $B^{T}$ 就是：

$$
B = \begin{pmatrix}
-t &-u &-v &\\
 t & u & v &\\
 t & u & v &
\end{pmatrix}
$$

$t,u,v$ 为任意常数
## 非齐次矩阵方程

> 非齐次总是蕴含齐次，下面是一个很好的例子

设 $A$ 为三阶矩阵，$b = (9,18,-18)^{T}$  非齐次线性方程组 $Ax = b$ 的 通解为 

$$
k_{1}(-2,1,0)^{T} + k_{2}(2,0,1)^{T} + (1,2,-2)^{T}
$$

其中 $k_{1},k_{2}$ 为任意常数， 求 $A$ 

我们先思考齐次方程组 $Ax = 0$ 他的解空间的两个基是：

$$
\alpha_{1} = (-2,1,0),\alpha_{2} =(2,0,1)
$$

这就对应着这个同解方程：

$$
\begin{pmatrix}
1 & 2 & -2 \\
0 & 0 & 0  \\
0 & 0 & 0
\end{pmatrix} x = 0
$$

> [!tip]
> 当然，我们也可以通过求正交来得到，知道了 $\alpha_{1}$ 和 $\alpha_{2}$ ，那么 $A$ 某一行就可以算出了：
>    $$
> Ax = \begin{pmatrix}
> l_{1}^{T} x \\
> l_{2}^{T} x \\
> \dots \\
> l_{n}^{T} x
> \end{pmatrix} = \begin{pmatrix}
> 0 \\
> 0 \\
> \dots \\
> 0
> \end{pmatrix}
> $$
> 可以留着验算，其实下面的计算量也不复杂。

这个就是反号顺抄写的逆运算，实际上因为 $r(A)=1$  我们就能够得到，最后的行最简是：

$$
\begin{pmatrix}
1 & a & b \\
0 & 0 & 0 \\
0 & 0 & 0
\end{pmatrix}
$$

只要基本功扎实，这个直接就秒了。或者代入都行，$a$ 和 $b$ 就直接出来了。由于 $r(A)=1$ 设：

$$
A = \begin{pmatrix}
1 a & 2a & -2a\\
1 b & 2b & -2b\\
1 c & 2c & -2c
\end{pmatrix}
$$
或者连行最简都想象不到，那就直接设：

$$
A = \begin{pmatrix}
a & b & c \\
ma & mb & mc \\
na & nb & nc \\
\end{pmatrix}
$$

那就求解 $Ax = 0$ 

自然能够化到 :

$$
\begin{pmatrix}
a & b & c \\
0 & 0 & 0 \\
0 & 0 & 0
\end{pmatrix} \to \begin{pmatrix}
1 & \frac{b}{a} & \frac{c}{a} \\
0 & 0 & 0 \\
0 & 0 & 0
\end{pmatrix}
$$

进而得到 $b=2a,c=-2a$ 那么：

$$
A = \begin{pmatrix}
a & 2a & -2a \\
ma & 2ma & -2ma \\
na & 2na & -2na
\end{pmatrix}
$$

那么再回到非齐次 $Ax =b$ 我们随便代入一个解，$k_{1},k_{2}$ 直接取 $0$  

$$
A \begin{pmatrix}
1 \\
2 \\
-2
\end{pmatrix} = \begin{pmatrix}
9a \\
9ma \\
9na
\end{pmatrix} =  \begin{pmatrix}
9 \\
18 \\
-18
\end{pmatrix}
$$

所以 $a = 1,m = 2,n =-2$ 

那么 

$$
A = \begin{pmatrix}
1 & 2 & -2 \\
2 & 4  & -4 \\
-2 & -4 & 4
\end{pmatrix}
$$

当然，如果连分离齐次和非齐次都没有想到，那么最直觉的操作，把这个线性方程组转为矩阵方程，然后求解出系数矩阵：

$$
(Ax_{1},Ax_{2},Ax_{3}) = (b,b,b) , A(x_{1},x_{2},x_{3}) = (b,b,b)
$$

其中：

$$
(x_{1},x_{2},x_{3}) = \begin{pmatrix}
-1 & 3 & 1 \\
3 & 2 & 2 \\
-2 & -1 & -2
\end{pmatrix}
$$

> 实际上， 非齐次线性方程组有 $n -r(A) + 1$  个线性无关的解向量。

这是可逆的，那么：

$$
A = (b,b,b)(x_{1},x_{2},x_{3})^{-1}
$$

那么计算量就来到了 $x_{1},x_{2},x_{3}$ 的逆上了。 


如果不可逆，那就要回到上面齐次矩阵方程中的，转置的操作，换一下顺序，把$A$ 放到未知数上，$A$ 的每一列都是 $X^{T}a=b$    解向量，根据基础解系得到每一列的表达式，然后再根据其他信息确定了。

> [!tips]  特征值、特征向量反求矩阵

我们还有一种思路，就是思考到他的相似对角化，因为 [[秩一矩阵]]的特征向量和特征矩阵都非常容易得到。并且这里明确的说明  $Ax =0$ 的解。这一点我们在[[特征值和特征向量]]也特地提出过，对于 $Ax = 0$ 要由特征值特征向量的敏感性，即使没有秩一，我们也应该想到。

对于$0$ 特征值，对应两个线性无关的特征向量：

$$
A(-2,1,0) = 0,A(2,0,1) =0
$$

接下来是 $tr(A)$ 和 $\alpha$  这一对。我们看到非齐次线性方程组：

$$
Ax = \begin{pmatrix}
9 \\
18 \\
-18
\end{pmatrix} = 9 \begin{pmatrix}
1 \\
2 \\
-2
\end{pmatrix}
$$

显然 $\alpha = (1,2,-2)^{T}$ ，对应的特征值为 $9$ 

因此，我们可以根据对角矩阵和特征向量计算$A$ 

$$
A = P\Lambda P^{-1}
$$

我们也可以设 $\beta = (a,b,c)$ 求解三个未知数，思路是先消元然后代入 $tr(A)$ 计算（要充分利用上我们的秩一矩阵的已知信息，来简化计算）

# 解的关系

> 公共解，同解，交集，子集。一方面是选择题，通过分块矩阵判断秩。另一方面公共解、同解解出参数
> 要把解空间的关系，转化为线性方程组秩之间的关系，实际上是回到我们的系数矩阵空间的关系。而空间则是我们上一章向量所重点研究的问题。
> 这里就蕴含，（非）齐次方程组解的判定。

## 例题1 分析秩 #failed

> 要在平时锻炼取反例的能力。这一题想要正面说明异常困难！！！

设三维矩阵 $A$，$B$ 满足 $r(AB) = r(BA) + 1$，则

(A) 方程组 $(A+B)x = 0$ 只有零解。

$$
r(A + B) = 3
$$

(B) 方程组 $Ax = 0$ 与方程组 $Bx = 0$ 均只有零解。

 $r(A) = 3, r(B) = 3 \to r(AB) = r(BA) = 3$ 矛盾

(C) 方程组 $Ax = 0$ 与方程组 $Bx = 0$ 没有公共非零解。

$$
r \begin{pmatrix}
A \\
B
\end{pmatrix} = 3
$$

实际上这里，要求判断是否一定有 $row(A) \cap row(B) = \emptyset$ ，在$r(AB) = r(BA) +1$ 的情况下。我们秩和空间的关系为：

$\dim\bigl(\operatorname{row}A\cap\operatorname{row}B\bigr)=r(A)+r(B)-r\!\begin{pmatrix}A\\B\end{pmatrix}=r(A)+r(B)-n.$

> 注意不是 $r(AB)$，实际上 $r(AB) = r(B) - dim(col B \cap ker A)$

也就是 $r(A) +r(B)$ 是否一定得要是 3 ，但是我们的条件只是说降维了。

从直观上理解，我们先关注于 $A$ ，那么对于 $A$ 来说，他的行向量对 $B$ 做 行的线性组合得到了 $AB$ 。同时他的列向量对 $B$ 的列做了线性组合得到 $BA$ 。由于 $A$ 不满秩，因此有效的 行（列）的线性组合的次数缺少导致了，$B$ 的 行列信息可能丢失，也就是会造成降秩的情况，这里存在不确定性，到底降秩还是秩不变，需要具体的 $A$ 来说明。因此虽然 $AB$ 和 $BA$ 对 B 做的 有效变换（唯一约束是行秩等于列秩）的次数是一致的，但是，效果如何并不确定，这里的情况是，AB相较BA保留了更多信息。

实际上，我们考察题目之间的数量关系。首先我们得到：

$r(AB)=\dim A(\operatorname{im}B)=r(B)-\dim\bigl(\operatorname{im}B\cap\ker A\bigr).$

> im B 就是 col(B)

也就是

$r(AB)=r(B)-\dim\bigl(\operatorname{row}B\cap\ker A^\top\bigr).$

同理得到 $r(BA)$ ，进一步我们得到 $r(AB) - r(BA)$

$r(AB)-r(BA)=\bigl[r(B)-r(A)\bigr]+\Bigl(\,\dim(\operatorname{im}A\cap\ker B)-\dim(\operatorname{im}B\cap\ker A)\Bigr).$

当然这里还有一个约束是，$r(A) \le 2,r(B)\le 2$

当然我们猜测这里的条件是宽松，没有办法给一个强大 $r(A)+r(B) = 3$ 的约束，而要说明他的错误，就需要举出反例了。

(D) 方程组 $ABAx = 0$ 与方程组 $BABx = 0$ 有公共非零解。

$$
r \begin{pmatrix}
ABA \\
BAB
\end{pmatrix} < 3
$$

我们一眼看过去能够分析的就是 $B$ 选项。对于选择题，我们应该分析一下选项之间的关系 #tip 

对于 C和A，如果我们能够说明 有一个 $x$ 使得 $Ax = 0$   并且使得 $Bx = 0$ 那么这个 $x$ 一定可以使得 $(A+B)x = 0$ 。这时候，我们的 $A$ 和 B 的 秩 应该是充分小的。 考虑举例子，首先对于 $AB$ 和 $BA$ 我们先定下 $A$ 写出 $B$ ，那么设：

$$
A = \begin{pmatrix}
1 & 0 & 0 \\
0 & 0 & 0 \\
0 & 0 & 0
\end{pmatrix}
$$

我们期望 $r(AB) = 1,r(BA) = 0$  ，那么 $BA$ 是按照 $B$ 的每一行来给A做行的线性组合，显然 要使得 $BA = \mathbf{O}$   我们需要第一行不取，其余的任意，也就是对于 B 的每一行来说，第一个元素需要是0：

$$
B = \begin{pmatrix}
0 &  &  \\
0 &  &  \\
0 &  & 
\end{pmatrix}
$$

进一步AB列变换，这是按照 B的每一列对 $A$ 做列的线性组合，那我们只需要B的每一列的第一个元素（对应A的第一列）是自由即可。 最简单的，我们得到一个B：

$$
B = \begin{pmatrix}
0 & 1 & 0 \\
0 & 0 & 0 \\
0 & 0 & 0
\end{pmatrix}
$$

> 需要对矩阵乘法的两种形式熟悉，我们才能够快速的构造一个 $B$ 出来

显然 $r(A) +r(B) = r\begin{pmatrix}A \\ B\end{pmatrix} = 2$  根据排除法，我们得到 $D$ 的正确性。

对于 $D$ ： 方程组 $ABAx = 0$ 与方程组 $BABx = 0$ 有公共非零解。这是一个存在的表述，我们要抓住任意性。 #tip 

两个方向：

- 找到一个 $x$ 和 初始条件不矛盾
	- 满足 $ABAx = 0$ 
	- 满足 $BABx = 0$
- 假定 $x$ 满足 $ABAx = 0$ 
	- 查看满足 $BABx$ 需要什么条件 
	- 考察初始条件是否能够推出


## 例题2 伴随矩阵 #failed 



# 基础解系

> - 要抓住 系数矩阵的极大无关组和基础解系 对 $n$ 维空间的 完备分割。
> - 基础解系需要说明秩，因此回到矩阵形式也是一个不错的角度和想法
> - 进一步的，我们要把握住，非齐次和他对应的齐次方程组的解之间的关系。这里齐次方程组解的性质蕴含在非齐次的分析当中。
> 	- 非齐次方程组没有基础解系的概念，因此对于 $r(A)$ 和 解空间的分析要回到导出组 $Ax =0$ 上说明。
> 	- 要抓住 互不相等的解，互相独立的解，两种情况：
> 		- 互不相等做差，就能够构建出一个 非零的解
> 		- 互相独立 才能够称为 基础解系

## 普通矩阵方程

设 三阶矩阵 $A$ 的 第一行为 $(a,b,c)$, $a,b,c$ 不全为零， $B = \begin{pmatrix}1 & 2 & 3 \\ 2 & 4 & 6 \\ 3 & 6 & k\end{pmatrix}$ 满足 $AB = O\mathbf{}$ 求 线性方程组 $Ax = 0$ 的通解

首先是分析秩，注意到给出了 $A$ 的 三阶 形状限制，大概率要围绕这里做讨论。不全为零保证了 $r(A) \ge 1$, 对于 $B$ 他的第一列，第二列成比例，并且可以观察到 $k = 9$ 时，我们的 $r(B) = 1$ 对于 矩阵方程而言：

$$
AB = \mathbf{O} \to r(A) +r(B) \le 3
$$

因此我们分类为，$k = 9$ 和 $k\neq 9$ 。先分析简单的，$k \neq 9$ 我们能够得到 $r(B) = 2$ 此时 $r(A)$ 只能取1 了，那么我们就得到，$B$ 的 极大无关组恰好就是 $A$ 的 基础解系的解向量：

$$
k_{1} \begin{pmatrix}
1 \\
2 \\
3
\end{pmatrix} + k_{2} \begin{pmatrix}
3 \\
6 \\
k
\end{pmatrix}
$$
当 $k = 9$ 时候，$r(B) = 1$

- $r(A) =2$ ，此时基础解系为 $k_{3} \begin{pmatrix}1 \\ 2 \\ 3\end{pmatrix}$
- $r(A) = 1$  直接求解 $A x = 0$ ，用 $a,b,c$ 表示

## 抽象基础解系

设 $A=(a_i)$不可逆，$a_{12}$的代数余子式 $A_{12} \neq 0$, $a_1, a_2, a_3$ 为矩阵 $\mathbf{A}$ 的列向量组, $\mathbf{A}^*$ 为 $\mathbf{A}$ 的伴随矩阵, 则方程 $\mathbf{A}\mathbf{x}=\mathbf{0}$ 的通解为 ( )

A. $x=k_1a_1+k_2a_2+k_3a_3$,其中 $k_1,k_2,k_3$ 为任意常数.

B. $x=k_1a_1+k_2a_2+k_3a_3$,其中 $k_1,k_2,k_3$ 为任意常数.

C. $x=k_1a_1+k_2a_2+k_3a_3$,其中 $k_1,k_2,k_3$ 为任意常数.

D. $x=k_1a_1+k_2a_2+k_3a_3$,其中 $k_1,k_2,k_3$ 为任意常数.

这里的关键是 借助了 $A_{12} \neq 0 \to r(a_{1},a_{3},a_{4}) = 3$ 

## 非齐次与齐次导出组 秩



设n阶矩阵A的伴随矩阵$A^* \neq O$，ξ₁,ξ₂, ..., ξₙ 为非齐次线性方程组 $Ax = b$ 的互不相等特解。则 $Ax = b$ 的线性无关解向量的个数为____。

非齐次方程组互不相等的特解就能够导出 $Ax = 0$ 有一个非零解，而对于伴随矩阵而言只有三种秩的情况，满秩，秩一，$\mathbf{O}$ 因此这里 $r(A^{\star}) = 1 \to r(A) = n-1$

因此线性无关的解向量的个数为2

设3阶矩阵 $A = (\alpha, \alpha, \alpha)$ 有三个不同的特征值，其中 $\alpha_3 = \alpha_1 + 2\alpha_2$。

(I) 证明 $r(A) = 2$；

回到 $Ax = 0$ ，注意到特征值，我们自然联系到 这是一个 特征方程组，由于 三个不同的特征值，因此每个特征方程解空间都是一维的。进一步 由于 $a_{3} = a_{1} + 2a_{2} \to a_{1} +2a_{2} - a_{3} = 0$

也就是 $A \begin{pmatrix}1 \\ 2 \\ -1\end{pmatrix} = 0$

也就是 特征方程 $Ax = 0$ 存在，此时 解空间为1，因此 $r(A)= 2$

(II) 若 $\beta = \alpha_1 + \alpha_2 + \alpha_3$，求线性方程组 $Ax = \beta$ 的通解。

通解 = 齐次的通解 + 非齐次的特解


设 $A$ 为 4阶矩阵，$k$ 为任意常数，$\eta_{1},\eta_{2},\eta_{3}$ 为齐次方程组$Ax = b$ 的三个解，满足
$n_1+n_2 = \begin{pmatrix} 1 \\ 2 \\ 3 \\ 4 \end{pmatrix}$, $n_2+2n_3 = \begin{pmatrix} 2 \\ 3 \\ 4 \\ 5 \end{pmatrix}$。若r(A)=3, 则Ax=b的通解为【 】。 

$2\eta_{3} - \eta_{1} = \begin{pmatrix}1 \\ 1 \\ 1 \\ 1\end{pmatrix}$ 是 $Ax = b$ 的一个解，又因为 $r(A) =3$ 因此非齐次方程组的特解为

$$
\begin{pmatrix}
1 \\
1 \\
1 \\
1
\end{pmatrix}
$$

下面考察$Ax =0$ 的通解，需要两个 $Ax = b$ 的特解相减，我们可以通过 $\frac{1}{2} \eta_{1} + \frac{1}{2}\eta_{2} = \frac{1}{2} \begin{pmatrix}1 \\ 2 \\ 3 \\ 4 \end{pmatrix}$ 得到，因此 $\begin{pmatrix}1 \\ 1 \\ 1 \\ 1 \end{pmatrix} - \begin{pmatrix} \frac{1}{2} \\ 1 \\ \frac{3}{2} \\ 2\end{pmatrix}$ 那么对于齐次特解我们可以 任意乘上常数，得到特解为：

$$
\begin{pmatrix}
1 \\
0 \\
-1 \\
-2
\end{pmatrix}
$$

因此，我们得到非齐次通解为：

$$
\begin{pmatrix}
1 \\
1 \\
1 \\
1
\end{pmatrix}  + k_{1} \begin{pmatrix}
1 \\
0 \\
-1 \\
-2
\end{pmatrix}
$$


由于特解的不同，我们得到的通解也不同，如何说明两个通解是等价的，以通解备择1:

$$
\begin{pmatrix}
1 \\
2 \\
3 \\
4
\end{pmatrix} + k_{2} \begin{pmatrix}
-1 \\
0 \\
1 \\
2
\end{pmatrix}
$$

齐次通解部分，我们看是否成比例。而特解部分，由于特解满足 $Ax = b$ 因此我们检查：

$$
\begin{pmatrix}
1 \\
2 \\
3 \\
4
\end{pmatrix} = \begin{pmatrix}
1 \\
1 \\
1 \\
1
\end{pmatrix} + k \begin{pmatrix}
1 \\
0 \\
-1 \\
-2
\end{pmatrix}
$$

是否有解，显然是无解的，我们可以写出一个等价的通解为：

$$
r(A)+r(B)r(A)+r(B)\begin{pmatrix}
0 \\
1 \\
2 \\
3
\end{pmatrix} + k \begin{pmatrix}
-1 \\
0 \\
1 \\
2
\end{pmatrix}
$$
